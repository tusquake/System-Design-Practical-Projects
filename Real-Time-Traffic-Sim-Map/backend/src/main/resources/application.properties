spring.application.name=traffic-backend
server.port=8080

# Redis Configuration (assuming local Redis for development)
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
